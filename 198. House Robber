class Solution {
    public int rob(int[] nums) {

        int prev2 = 0; // i-2
        int prev1 = 0; // i-1

        for (int num : nums) {
            int curr = Math.max(prev1, num + prev2);
            prev2 = prev1;
            prev1 = curr;
        }

        return prev1;
    }
}
